syntax = "proto3";

package awfdata;

import "terrain.proto";

message Map {
    uint32 width = 1;
    uint32 height = 2;

    message Tile {
        Terrain terrain = 1;
    }

    // To access <X,Y>, access [Y*width + X]
    repeated Tile tiles = 3;
}
